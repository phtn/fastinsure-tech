import "./rays.css";

const GodRays = () => (
  <div className="transition-bg relative flex h-screen flex-col items-center justify-center bg-white dark:bg-black">
    <div className="absolute inset-0 left-[-60vw] top-[-90vh] overflow-hidden">
      <div className="jumbo absolute -inset-[10px] left-[10vh] opacity-50"></div>
    </div>
    <div className="mt-4"></div>
  </div>
);

export default GodRays;
